package edu.jorge.proyectodaw.controller.dto.input;

// import javax.validation.constraints.Min;
// import javax.validation.constraints.Max;

// @Valid
public class FeatureProductInputDTO {

    private Long id;

    // @Min(value = 1, message = "The value must be greater than or equal to 1")
    // @Max(value = 5, message = "The value must be less than or equal to 5")
    private Integer value;
    
}
